# 2026-09-01

* Чтение JSON через `json.load`;
* Путь к файлу через `Path(__file__).parent`;
* Обработку `FileNotFoundError`;
* Обработку `json.JSONDecodeError`;
* Проверку, что JSON верхнего уровня — список;
* Фильтрацию плохих записей внутри списка;
* Проверку обязательных полей;
* Проверку типов полей;
* `main()` и `if __name__ == "__main__"`.

# 2026-08-31

* Словари - `dict`;
* Множество - `set`;
* Генератор списка - `list comprehension`;
* Генератор множества - `set comprehension`;
* Генератор словаря - `dict comprehension`;
* Группировку по device_id;
* Статистику по event_type;
* Поиск дубликатов event_id.

# 2026-08-29

Повторение Python продолжается

- Использовал словарь `dict` (`{ key: value }`)
- Использовал список `list` (`[ значение, значение ]`)
- Использовал `for` с методом `.items()` для словаря
- Проведен рефакторинг, весь код вынесен в функции

# 2026-08-27

Продолжаем вспоминать базу Python:

- Булеву логику: True, False, and, or, not, сравнения
- Условия
- Циклы
- Функции
- Создана функция для сбора ошибок от сканера

# 2026-08-26

Вспомнил основы Python:

- Ввод/вывод информации в консоль
- Типы данных
- Виртуальное окружение `.venv`
- Создан репозиторий для обучения