# Дневник обучения

## 2026-09-07

- Изучили `dataclass`
- Разобрали `field(default_factory=...)`
- Переписали `Stats` из `TypedDict` в `dataclass`
- Заменили доступ к статистике со словаря на объект

## 2026-09-06

- Перенесли `Stats` в `models.py`
- Удалили лишний файл `schema_data.py`
- Обновили импорты после переноса типов

## 2026-09-03

- Изучили `type hints` и добавили аннотации типов в функции проекта
- Разобрали `TypedDict` для описания структуры словарей
- Создали типы `EventItem` и `Stats`
- Поняли, почему не стоит называть файл `types.py`: такое имя конфликтует со стандартным модулем Python
- Вынесли структуры данных в `schema_data.py`, чтобы использовать общие типы в разных модулях проекта
- Обновили `README.md` и добавили описание структуры проекта

## 2026-09-02

- Разделили код на модули
- Создали `loader.py`
- Создали `validation.py`
- Создали `event_statistics.py`
- Создали `reporting.py`
- Создали `processor.py`
- Оставили `main.py` как точку входа
- Добавили `README.md`

## 2026-09-01

- Реализовали чтение JSON через `json.load`
- Настроили путь к файлу через `Path(__file__).parent`
- Добавили обработку `FileNotFoundError`
- Добавили обработку `json.JSONDecodeError`
- Добавили проверку, что JSON верхнего уровня — список
- Реализовали фильтрацию плохих записей внутри списка
- Добавили проверку обязательных полей
- Добавили проверку типов полей
- Оформили `main()` и `if __name__ == "__main__"`

## 2026-08-31

- Изучили словари (`dict`)
- Изучили множества (`set`)
- Изучили генератор списка (list comprehension)
- Изучили генератор множества (set comprehension)
- Изучили генератор словаря (dict comprehension)
- Реализовали группировку по `device_id`
- Реализовали статистику по `event_type`
- Реализовали поиск дубликатов по `event_id`

## 2026-08-29

Повторение Python продолжается:

- Использовали словарь `dict` (`{ key: value }`)
- Использовали список `list` (`[ значение, значение ]`)
- Использовали `for` с методом `.items()` для словаря
- Провели рефакторинг: весь код вынесен в функции

## 2026-08-27

Продолжили вспоминать базу Python:

- Изучили булеву логику: `True`, `False`, `and`, `or`, `not`, сравнения
- Изучили условия
- Изучили циклы
- Изучили функции
- Создали функцию для сбора ошибок от сканера

## 2026-08-26

Вспомнили основы Python:

- Ввод и вывод информации в консоль
- Типы данных
- Виртуальное окружение `.venv`
- Создали репозиторий для обучения
